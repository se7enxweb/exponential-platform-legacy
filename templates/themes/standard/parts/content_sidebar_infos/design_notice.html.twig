{# ── Design toggle notice ── #}
<div class="nw-design-notice">
    <strong>📐 App theme (fallback)</strong><br>
    To enable the rich design, add a <code>#</code> comment to <code># - sevenx_content_website_app_developer</code> and <code># - sevenx_content_website_app_info</code>
    to <code>design_list → site</code> in <code>ezpublish_siteaccess.yaml</code>,
    then clear cache.
</div>
