{#
    Minimal field templates for the sevenx-content-website-app theme.
    Overrides the frontend_group inherited template (themes/app/content/parts/content_fields.html.twig)
    which requires Netgen Site API Twig functions not available in this siteaccess context.
    Falls through to eZ Platform core field rendering for everything not defined here.
#}
